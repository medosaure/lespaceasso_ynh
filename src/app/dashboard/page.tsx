// Cette page est maintenant vide car le contenu est géré par le layout du dashboard.
// Le fichier layout.tsx dans ce même dossier contient la structure.
export default function DashboardPage() {
  return null;
}
